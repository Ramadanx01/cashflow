# إعداد النسخ الاحتياطي إلى Google Drive

تخزين التطبيق الأساسي يظل في IndexedDB على الجهاز. هذا التكامل يرفع أو يستعيد ملف JSON فقط عند طلب المستخدم، ولا يزامن البيانات تلقائيًا ولا يحتاج خادمًا. لا يستطيع الموقع قراءة حسابات Google المسجلة في المتصفح تلقائيًا؛ المستخدم يختار الحساب ويمنح الإذن عند الربط الأول.

## إعداد Google Cloud

1. أنشئ مشروعًا في Google Cloud Console.
2. فعّل Google Drive API وGoogle Picker API للمشروع.
3. جهّز شاشة OAuth consent، وأضف حساب Google إلى Test users أثناء الاختبار.
4. أنشئ OAuth Client ID من النوع Web application.
5. أضف `http://localhost:3000` إلى Authorized JavaScript origins للتطوير. عند استخدام عنوان محلي آخر، أضف الـorigin نفسه.
6. أضف origin موقع GitHub Pages، مثل `https://USERNAME.github.io`. لا تضف مسار المستودع بعد الشرطة المائلة.
7. أنشئ API key، وقيّده على مواقع التطبيق، واسمح باستخدام Google Picker API فقط.
8. انسخ OAuth Client ID وAPI key ورقم Project Number إلى كائن `CONFIG` أعلى `drive-backup.js`:

```js
const CONFIG = {
  clientId: 'YOUR_WEB_CLIENT_ID.apps.googleusercontent.com',
  apiKey: 'YOUR_RESTRICTED_API_KEY',
  appId: 'YOUR_GOOGLE_CLOUD_PROJECT_NUMBER'
};
```

معرّف العميل ومفتاح API ورقم المشروع معرفات عامة للمتصفح وليست أسرارًا؛ قيّد مفتاح API على نطاق الموقع وواجهات Google المطلوبة. لا تضع OAuth Client Secret في ملفات الموقع.

## الاستخدام

- افتح إعدادات العمولات والنسخ الاحتياطي مع اتصال بالإنترنت.
- اختر «ربط حساب Google» مرة واحدة، واختر الحساب ووافق على الصلاحيات.
- عند أول نسخة اختر «حفظ نسخة على Drive»، ثم حدد مجلد Drive. يحفظ التطبيق البريد ومعرّف المجلد محليًا حتى يستخدمهما لاحقًا. ويمكن تغيير المجلد أو إلغاء الربط من الإعدادات.
- للاستعادة اختر «استعادة من Drive» ثم ملف JSON. يتحقق التطبيق من الملف ويطلب تأكيدًا قبل استبدال بيانات IndexedDB.

تُطلب صلاحيات `drive.file` و`openid email`. يحفظ التطبيق بريد الحساب ومعرّف المجلد في IndexedDB، ولا يحفظ رمز الوصول؛ يطلب رمزًا مؤقتًا عند كل عملية باستخدام الحساب المرتبط. تبقى وظائف التطبيق المحلية وملفات JSON المحلية متاحة دون Google أو إنترنت.

## حدود مهمة

- النسخ يدوي، وليس مزامنة بين الأجهزة أو رفعًا تلقائيًا في الخلفية.
- أثناء اختبار شاشة OAuth في وضع Testing، يجب إضافة حساب المستخدم ضمن Test users.
- يرفع التطبيق نسخة جديدة في كل مرة ولا يحذف النسخ السابقة من Drive.
- إلغاء الربط يمسح الحساب والمجلد المحفوظين في التطبيق، لكنه لا يلغي موافقة OAuth من إعدادات حساب Google ولا يحذف ملفات Drive السابقة.
- لا تضع بيانات OAuth في مستودع عام قبل تقييد API key وإعداد origins المسموح بها.